<h1>固定費登録</h1>
<table>
  <tr>
    <th>日付</th>
    <th>収入科目</th>
    <th>金額</th>
    <th>詳細</th>
  </tr>
  <%= form_with model: @form, url: fixedcost_values_path, local:true do |form| %>
    <%= form.fields_for :fixedcost_values do |f| %>
      <tr>
        <td><%= f.date_field :year_month %></td>
        <td><%= f.collection_select :fixedcost_id, @fixedcosts, :id, :name, include_blank: "選択して下さい" %></td>
        <td><%= f.text_field :value %>円</td>
        <td><%= f.text_field :description %></td>
      </tr>
    <% end %>
    <%= form.submit %>
  <% end %>
</table>